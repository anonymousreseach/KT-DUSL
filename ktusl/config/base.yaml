# ktusl/config/base.yaml
data:
  traces_csv: ktusl/data/traces.csv
  subjects_csv: ktusl/data/subject_metadata.csv

split:
  test_frac: 0.2

filter:
  level: 3
  first_attempt_only: true

eval:
  combine_mode: mean         # mean | noisy_or | noisy_and | logit_linear
  save_predictions: null
  save_metrics: outputs/metrics_log.csv   # <--- nouveau
